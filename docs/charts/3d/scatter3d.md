# Scatter Chart 3D

<div class="lang-en">

## Signature

`sp.scatter3d(title, x=None, y=None, z=None, *, color_hex=0x6366F1, palette=None, bg_color="#1a1a2e", width=900, height=600, x_label="", y_label="", z_label="", **kwargs) -> Chart`

Aliases: `sp.build_scatter3d_chart()`, `sp.scatter_3d()`, `sp.scatter3d_chart()`.

## Description

`sp.scatter3d()` scatters individual points in 3D space, useful for clustering and outlier/regression dream methods.

## Parameters

<div data-sp-registry-table="options" data-family="scatter_3d"></div>

## Returns

`Chart` object with an `.html` property and a `.show()` method.

</div><!-- /lang-en -->

<div class="lang-fr" style="display:none">

<h2>Signature</h2>

`sp.scatter3d(title, x=None, y=None, z=None, *, color_hex=0x6366F1, palette=None, bg_color="#1a1a2e", width=900, height=600, x_label="", y_label="", z_label="", **kwargs) -> Chart`

Alias : `sp.build_scatter3d_chart()`, `sp.scatter_3d()`, `sp.scatter3d_chart()`.

<h2>Description</h2>

`sp.scatter3d()` disperse des points individuels dans l'espace 3D, utile pour le clustering et les méthodes de régression/outliers.

<h2>Paramètres</h2>

<div data-sp-registry-table="options" data-family="scatter_3d"></div>

<h2>Retour</h2>

Objet `Chart` avec une propriété `.html` et une méthode `.show()`.

</div><!-- /lang-fr -->
