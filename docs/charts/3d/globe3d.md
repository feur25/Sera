# Globe Chart 3D

<div class="lang-en">

<style>
.lang-en table,.lang-fr table{width:100%}
</style>

## Signature

`sp.globe3d(title, lats=None, lons=None, values=None, *, project="orthographic", bg_color="#0a0a1a", width=900, height=600, **kwargs) -> Chart`

Aliases: `sp.build_globe3d_chart()`, `sp.globe_3d()`, `sp.globe3d_chart()`, `sp.globe()`.

## Description

`sp.globe3d()` plots geo-located values as a heat-colored 3D globe.

## Parameters

<div data-sp-registry-table="options" data-family="globe"></div>

## Returns

`Chart` object with an `.html` property and a `.show()` method.

</div><!-- /lang-en -->

<div class="lang-fr" style="display:none">

<h2>Signature</h2>

`sp.globe3d(title, lats=None, lons=None, values=None, *, project="orthographic", bg_color="#0a0a1a", width=900, height=600, **kwargs) -> Chart`

Alias : `sp.build_globe3d_chart()`, `sp.globe_3d()`, `sp.globe3d_chart()`, `sp.globe()`.

<h2>Description</h2>

`sp.globe3d()` place des valeurs géolocalisées sur un globe 3D coloré façon carte de chaleur.

<h2>Paramètres</h2>

<div data-sp-registry-table="options" data-family="globe"></div>

<h2>Retour</h2>

Objet `Chart` avec une propriété `.html` et une méthode `.show()`.

</div><!-- /lang-fr -->
