# KDE Chart 3D

<div class="lang-en">

## Signature

`sp.kde3d(title, categories=None, values=None, *, x_label="", y_label="", z_label="", bg_color="#1a1a2e", width=900, height=560, **kwargs) -> Chart`

Aliases: `sp.build_kde3d_chart()`, `sp.kde_3d()`, `sp.kde3d_chart()`, `sp.density3d()`.

## Description

`sp.kde3d()` renders kernel-density estimate ridges as layered 3D surfaces, one per category.

## Parameters

<div data-sp-registry-table="options" data-family="kde3d"></div>

## Returns

`Chart` object with an `.html` property and a `.show()` method.

</div><!-- /lang-en -->

<div class="lang-fr" style="display:none">

<h2>Signature</h2>

`sp.kde3d(title, categories=None, values=None, *, x_label="", y_label="", z_label="", bg_color="#1a1a2e", width=900, height=560, **kwargs) -> Chart`

Alias : `sp.build_kde3d_chart()`, `sp.kde_3d()`, `sp.kde3d_chart()`, `sp.density3d()`.

<h2>Description</h2>

`sp.kde3d()` rend des crêtes d'estimation de densité par noyau sous forme de surfaces 3D superposées, une par catégorie.

<h2>Paramètres</h2>

<div data-sp-registry-table="options" data-family="kde3d"></div>

<h2>Retour</h2>

Objet `Chart` avec une propriété `.html` et une méthode `.show()`.

</div><!-- /lang-fr -->
